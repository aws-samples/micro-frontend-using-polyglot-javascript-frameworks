{
    "name": "charts-data-lambda",
    "version": "1.0.0",
    "description": "Lambda function to get data from Charts table",
    "private": true,
    "scripts": {
        "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outdir=./dist/"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.114",
        "@types/express": "^4.17.17",
        "@types/node": "*",
        "esbuild": "^0.17.18"
    },
    "dependencies": {
        "aws-lambda": "^1.0.7",
        "aws-sdk": "*",
        "express": "^4.18.2",
        "serverless-http": "^3.2.0"
    }
}
